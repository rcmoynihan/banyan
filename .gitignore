# OS / editor
.DS_Store
Thumbs.db
*.swp

# local scratch
*.local.*
tmp/

# Banyan local state
/.banyan/

# Claude Code agent worktrees (transient; never commit)
.claude/worktrees/

# python
__pycache__/
*.pyc

# eval run outputs (reproducible artifacts; keep the dir, not the runs)
eval/review-ab/results/*/

# run-visualizer tool deps (committed: package.json + package-lock.json; ignored: node_modules)
tools/run-visualizer/node_modules/
